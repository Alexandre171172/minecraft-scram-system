-- Configuration du systeme SCRAM
return {
  leverSide = "left",
  cardSide = "right",
  scramOutputSide = "back",
  monitorSide = nil, -- nil = premier moniteur trouve
  cardTimeout = 15,
  updateInterval = 0.1,
  textScale = 0.8
}
